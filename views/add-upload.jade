extends layout

block head
	link(rel='stylesheet', href='/stylesheets/style_upload.css')
	script(src="/javascripts/progress.js")

block content
	div.q
		h1 Upload to #{list.name}
		form#fform(method="post" enctype="multipart/form-data" onSubmit = "window.location='/p/#{list.id}'" action = "/api/#{list.id}/songs/upload")
			div.btn.button
				span Upload
				input#fi(type="file" name="song" accept="audio/*")
		div#progbarcontainer
			div#progbar
				div#progbarprogress
		p
			b#errormessage
